import Foundation

// Starting from the width and height of a screen in pixels, calculate the total number of pixels of the screen and store the result in another variable

let width: Int = 720
let height: Int = 480
let pixel: Int = width * height
print("The number of pixels are \(pixel)")
